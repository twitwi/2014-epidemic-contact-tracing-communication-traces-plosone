/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */

package com.heeere.networkpropagation;

/**
 *
 * @author twilight
 */
public interface Node {

    //public double lastTransitionTime(); // if we need to go beyond exponential (but then it can get even more complicated than that (e.g. integrated count of neighbors...))
    //public double countNeighbors(State state);

}
